server:
  port: 48081
  servlet:
    context-path: /sanwei

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sanwei_cli_dev?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&nullCatalogMeansCurrent=true&rewriteBatchedStatements=true
    username: root
    password: cy1959970441

  data:
    redis:
      host: localhost
      port: 6379
      database: 0

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true